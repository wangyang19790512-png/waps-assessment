# Anthropic API key (required for AI agent analysis)
ANTHROPIC_API_KEY=sk-ant-...

# API server port (default: 3001)
PORT=3001

# Frontend dev proxy target (only needed during development)
# VITE_API_URL=http://localhost:3001
